# AGENTS.md

## 개발 원칙

현재 사용자의 요청을 만족하는 가장 작고 유지보수 가능한 변경을 만든다.

넓게 생각하되, 좁게 행동한다.

- 기존 아키텍처, 의존성, 구현 패턴으로 해결할 수 있으면 우선 재사용한다.
- 현재 작업에 필요하지 않은 추상화, 의존성, 인프라, 호환 계층, 미래 기능을 추가하지 않는다.
- 미래 위험이나 개선점은 언급할 수 있지만, 자동으로 구현하거나 후속 Issue를 만들지 않는다.
- 작은 작업에는 Issue, ADR, 별도 계획서, 추가 Agent를 요구하지 않는다.
- 내부 구현 선택을 사용자에게 떠넘기지 말고, 가장 단순하고 합리적인 기술적 결정을 스스로 내린다.
- 테스트와 검증은 실제 변경된 동작과 구체적인 위험에 맞춰 수행한다. 이유 없이 광범위한 검증을 반복하지 않는다.
- 기존의 데이터 안전성, 보안, 공개 호환성 요구는 유지한다.
- 사용자 데이터, 공개 포맷/API, 보안 경계, 파괴적 파일 작업 등 되돌리기 어려운 계약을 변경할 때만 상위 검토로 올린다.
- 현재 작업이 요구하지 않는 한, 정상 동작하는 아키텍처를 재설계하지 않는다.
- 요청한 결과가 동작하고 관련 위험이 검증되면 작업을 종료한다.